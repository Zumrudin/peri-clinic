# /srv/peri/rebuild/.env — rebuild webhook receiver
REBUILD_PORT=8787
REBUILD_TOKEN=change-me-same-as-directus-env
BUILD_SCRIPT=/srv/peri/rebuild/build.sh
DEBOUNCE_MS=20000
STATE_FILE=/srv/peri/rebuild/state.json
# Optional: write build_log rows (Builder token, see directus/roles.md)
DIRECTUS_URL=http://127.0.0.1:8055
DIRECTUS_TOKEN=
