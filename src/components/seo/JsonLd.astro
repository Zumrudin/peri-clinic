---
interface Props {
  data: Record<string, unknown> | null | undefined;
}
const { data } = Astro.props;
---

{data && <script is:inline type="application/ld+json" set:html={JSON.stringify(data).replace(/</g, '\\u003c')} />}
