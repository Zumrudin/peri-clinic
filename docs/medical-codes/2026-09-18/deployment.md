# Публикация подтверждённых кодов

18.09.2026, по поручению пользователя опубликовать уверенно сопоставленные коды на dev и production.

Реализация: `43fcae5`. В `price_items` добавлены nullable-поля `medical_service_code` и `medical_service_name`. Заполнены только 93 строки со статусом «Соответствует описанному вмешательству» из текущего прайса. Условные варианты, неподтверждённые соответствия и архивные позиции не переносились. Поле `icd10` не изменялось.

Сопоставление выполнялось по паре `procedure.slug + name`, а не по совпадению ID между базами. До записи сохранены резервные копии; после записи проверено, что все прочие значения строк и число строк остались прежними. Подпись «Код медицинской услуги» отображается под названием конкретной позиции в общем прайсе и на странице процедуры. Точное название по номенклатуре сохранено в CMS и доступно в HTML-атрибуте `title` подписи.

| Среда | Публичный прайс | Релиз | Результат |
|---|---|---|---|
| Dev | https://peri.zumrudin.ru/uslugi-i-ceny | `/srv/peri/releases/2026-09-18T17-13-00` | 221 строка и 19 страниц процедур проверены; 93 кода |
| Production | https://prod.peri-clinic.zumrudin.ru/uslugi-i-ceny | `/srv/peri/releases/2026-09-18T20-15-40` | 221 строка и 19 страниц процедур проверены; 93 кода |

Проверки:

- `astro check` после совмещения с актуальным `main`: 0 errors, 0 warnings; 285 informational hints проекта.
- Обе сборки завершились успешно. В production также прошёл штатный SEO-контроль: 44 страницы, 43 URL sitemap, 0 ошибок и предупреждений.
- `scripts/qa/medical-service-codes.mjs` проверил каждый код, точное название, отсутствие кодов во всех исключённых строках и отсутствие неверной подписи «МКБ» на обоих внешних адресах.
- В браузере на dev проверены ширины 375/800/1440 px: код не обрезается, горизонтального переполнения нет. На production дополнительно проверена мобильная страница контурной пластики.
- Общая SEO-проверка локальной dev-сборки не была полностью зелёной из-за метаданных dev-контента, включая демонстрационные страницы специалистов; эти данные не изменялись в рамках добавления кодов. Production SEO-проверка прошла полностью.

Резервные копии данных:

- Dev: `/root/peri-clinnic.ru/scripts/migrate/out/medical-service-codes/1789751310426/`.
- Production: `/srv/peri/site/scripts/migrate/out/medical-service-codes/1789751718837/`.

Откат: восстановить только два новых поля из `before.json` (отсутствующее значение трактовать как `null`), предварительно сверив последующие редакторские изменения, затем пересобрать сайт. Добавленные nullable-поля можно оставить в схеме. Цены и прочие поля при откате не перезаписывать.

Основной исследовательский README описывает состояние до публикации; сведения выше фиксируют последующее разрешённое внедрение. Домены и DNS не переключались.
