# Выполнение

1. Сохранить исходную выгрузку и сверить все 290 услуг с текущими 221 строками.
2. Подготовить повторяемую миграцию, которая сохраняет бэкап, проверяет исходные
   значения, применяет 8 обновлений и 75 добавлений без удаления строк.
3. Добавить поля `price_max` и `price_group`, поддержку диапазонов и самостоятельных разделов.
4. Проверить типы, сборку, результат миграции и повторный dry-run без изменений.
5. Развернуть на dev и проверить внешний прайс, мобильную таблицу и отсутствие переполнения.
6. Выдать HTML-отчёт со всеми изменениями, сохранёнными и спорными позициями;
   проверить внешний GET, Content-Type и SHA-256.

## Результат

Выполнено на dev: 75 добавлений, 8 обновлений, 0 удалений, 296 строк.
Все значения проверены во внешнем HTML; навигация прошла на 320/375/800/1440 px
и без JavaScript. Прод не изменялся. Подробности: `docs/qa/yclients-prices/README.md`.
HTML-отчёт опубликован с проверкой GET 200, text/html и SHA-256.
