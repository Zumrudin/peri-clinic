# Photo frames for imported results

Selected gallery photos 13–21 are portrait composites displayed inside square frames, leaving side bars. Add a per-case CMS boolean `natural_frame` (default false), enabled only for the nine imported cases. For single-image cards it makes the frame follow the source ratio on mobile and desktop. Preserve originals, full before/after content, and existing layout for other cases. Apply in /result and procedure galleries.
