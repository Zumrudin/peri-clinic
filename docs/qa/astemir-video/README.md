# Видео Астемира: расследование 2026-09-14

Ветка `fix/astemir-video` создана от `main` (`3ada392`).

## Причина

Исходный файл `Астемир про био.MOV` (ID `5956a0a3-7e5a-4c05-b41d-c942e6d1a64b`) загружен в Directus в 12:28:39 UTC. Метаданные API: `video/quicktime`, 18 465 969 байт. Размер допустим, но формат не входит в разрешённые MP4/WebM в `src/lib/videoFiles.ts`.

Журнал `/root/.pm2/logs/peri-rebuild-out.log` содержит ошибки `Некорректное видео: требуется MP4/WebM до 50 МБ с метаданными файла.` в 12:29:09, 12:29:37 и 12:31:33 UTC. Стек указывает на `videoFileName` при генерации статических маршрутов. При неуспешной сборке `deploy/rebuild/build.sh` не переключает текущий релиз, поэтому посетитель продолжал видеть прежнюю страницу без нового видео.

## Состояние на момент проверки

До начала этого расследования материал уже был перепривязан к `biorevitalization-botashev.mp4` (ID `94b73cc0-edaa-43df-b8a2-8dd8d86eb057`, `video/mp4`, 18 482 375 байт, загружен в 12:38:57 UTC). У материала есть название, он связан с опубликованным специалистом `botashev`. Отсутствие отдельной обложки не мешает выводу: используется портрет специалиста.

Последняя проверенная публикация: build_log №90, успешно завершена в 13:24:54 UTC; релиз `/srv/peri/releases/2026-09-14T13-23-00`. HTML содержит карточку и ссылку на экспортированный MP4, файл присутствует в релизе.

Проверка Playwright/Chrome на https://peri.zumrudin.ru/specialisty/botashev при ширине 375 и 1440 px: одна видимая видеокарточка; после нажатия видео воспроизводится (`paused=false`, `currentTime>0.1`, `error=null`), длительность 48.234 секунды.

Новых изменений кода, данных CMS или публикации для восстановления этого видео не потребовалось. Для новых загрузок текущая реализация требует совместимый MP4/WebM до 50 МБ; автоматической конвертации MOV нет.
