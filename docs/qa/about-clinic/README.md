# Проверка блока «О клинике» — 13 сентября 2026

- `npm run check`: 0 ошибок, 0 предупреждений; существующие подсказки Astro о deprecated API.
- `npm run build`: успешная сборка, 43 страницы.
- `npm test`: 5 тестов пройдено.
- `scripts/qa/about-clinic.mjs`: 1440, 1024, 800, 375 px; циклическая прокрутка, отдельные группы, перетаскивание без открытия фото, возврат фокуса, страницы специалистов, описание под фото, изображения загружаются, нет переполнения. На телефоне проверена прежняя карусель результатов.
- `scripts/qa/about-clinic-edge.mjs`: 1/2 фотографии, скрытие навигации, настоящий touch через CDP, переход через край, удержание фокуса, axe — 0 нарушений в новом блоке и lightbox.
- Переходы «Подробнее» без JavaScript работают; демонстрационные страницы исключены из sitemap и имеют noindex.
- В публичном HTML/JS/JSON не обнаружены приватные параметры `access_token=`.

Скриншоты `about-*`, `specialist-*`, `lightbox-*` сняты при четырёх ширинах. В скриншотах блока скрыты фиксированные элементы общей оболочки для удобства просмотра. Карусели показаны после тестового пролистывания.

Проверка выполнена в явном локальном режиме `ABOUT_DEMO=true`. Настоящие CMS-коллекции пока не применены: доступный токен получил 403 при создании схемы. Подготовленная миграция требует прав администратора; затем необходима проверка загрузки реального контента и обновление snapshot/flows.
